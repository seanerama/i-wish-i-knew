{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:iwik:contracts:v1:tools:withdraw_contribution:input",
  "title": "withdraw_contribution input",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "run_ids"
  ],
  "properties": {
    "run_ids": {
      "minItems": 1,
      "maxItems": 100,
      "type": "array",
      "items": {
        "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
        "description": "ULID identifier",
        "type": "string"
      }
    },
    "reason": {
      "description": "Legacy free-text reason (stage 5). Never sent to the service: it is used only when it exactly equals a reason_code value.",
      "type": "string"
    },
    "reason_code": {
      "type": "string",
      "enum": [
        "member_request",
        "data_error",
        "policy_change"
      ],
      "description": "Withdrawal reason: member_request | data_error | policy_change"
    }
  },
  "description": "Withdraw your own runs; effective at the next evidence revision. Every run id must belong to your organization; the service never says which one was not. Scope: publish; side effect: remote_write."
}
