{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:iwik:contracts:v1:tools:report_outcome:input",
  "title": "report_outcome input",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "receipt_id",
    "observation",
    "observed_at"
  ],
  "properties": {
    "receipt_id": {
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID identifier",
      "type": "string"
    },
    "observation": {
      "additionalProperties": true,
      "description": "What was observed, in the protocol's vocabulary",
      "type": "object",
      "properties": {}
    },
    "observed_at": {
      "format": "date-time",
      "description": "RFC 3339 UTC timestamp",
      "type": "string"
    },
    "prediction_id": {
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID identifier",
      "type": "string"
    },
    "result": {
      "type": "string",
      "enum": [
        "met",
        "not_met",
        "indeterminate"
      ]
    },
    "environment_changed": {
      "type": "boolean"
    },
    "evaluation_run_id": {
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID identifier",
      "type": "string"
    }
  },
  "description": "Report the observed outcome of a prediction you registered earlier with register_prediction (prediction_id). result is met, not_met, or indeterminate; environment_changed records that the environment moved and is kept separate from the result. The stored prediction cannot be altered. receipt_id is the receipt the prediction was based on. Scope: publish; side effect: remote_write."
}
